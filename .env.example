# Used when running the API directly on the host.
DATABASE_URL=postgresql+psycopg://relay:relay@localhost:5432/relay

# Worker lease and retry policy (Compose supplies these defaults automatically).
JOB_LEASE_SECONDS=10
MAX_JOB_ATTEMPTS=3
RETRY_BASE_SECONDS=1
